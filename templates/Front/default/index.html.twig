{% extends 'Front/layout.html.twig' %}

{% block title %}Haskell Quiz/Home{% endblock %}
{% block body_class %}pt-5 fixed-sn mdb-skin-custom{% endblock %}
{% block main %}
    <h1 class="front-Top_Header display-4">
        <img src="{{ asset('img/Haskell-Logo.png') }}">
        <span class="front-Top_Header-text cyan-text">Haskell Quiz</span>
    </h1>
    <hr class="my-5">

    <section class="mb-5">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-body">
                        <p>
                            「すごいHaskellたのしく学ぼう!」に載っている関数のリストをQuiz形式でまとめたものです。
                        </p>
                        <p>
                            標準ライブラリではない関数については<a href="{{ path('front_functions') }}">関数リファレンス</a>を参考にしてください。
                        </p>
                        <p>
                            不要な空白は入れないでください。
                            (例)
                            × [1, 2, 3]
                            ○ [1,2,3]
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section>
        <div class="row">
            <div class="col-12 mb-4">
                <h1 class="front-Top_SectionHeader cyan lighten-5">
                    クイズ
                </h1>
            </div>
            {% for section in sections %}
                <div class="col-lg-4 col-md-12 mb-4">
                    <div class="card card-cascade">
                        <div class="text-center card-header cyan-gradient">
                            <h2 class="h2-responsive card-header-title mt-3 mb-4">{{ section.name }}</h2>
                        </div>
                        <div class="card-body text-center">
                            <p>{{ section.description }}</p>
                        </div>
                        <div class="card-footer text-center">
                            <a class="btn btn-primary btn-rounded waves-effect waves-light" href="{{ path('quiz_section', {slug: section.slug}) }}">GO!</a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </section>
{% endblock %}
