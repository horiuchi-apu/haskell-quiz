{% extends 'Admin/layout.html.twig' %}

{% block title %}Haskell Quiz 管理画面/セクション編集{% endblock %}

{% block navbar_title %}セクション編集{% endblock %}

{% block main %}
    <div class="card mb-3">
        <div class="card-header card-header-primary">
            セクション編集
        </div>
        <div class="card-body">
            {{ form_start(form) }}
            {{ form_rest(form) }}
            <button type="submit" class="btn btn-primary btn-block">登録</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}

